import Post from '../models/post_models.ts';
import type { Request, Response } from 'express';
import { authenticate } from '../middlewares/auth_Middleware.ts';

const isAuthor = async (req: Request, res: Response, next: Function) => {
    try {
        
    } catch (error) {
        
    }
}